Entity Metrics
==============

Entity-level metrics measure the distance between single point-in-time observations
within sequences.

Most of sequence-level metrics rely on an entity metric as a building block.
