[build-system]
requires = [
    "hatchling",
    "hatch-vcs",
    "wheel",
]
build-backend = "hatchling.build"

[project]
name = "TanaT"
description = "A library for temporal analysis of trajectories."
dependencies = [
    "matplotlib",
    "numba",
    "pandas",
    "polars",
    "scikit-learn",
    "scikit-survival",
    "tanat_utils",
]
requires-python = ">=3.9"
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
]
readme = "README.md"
dynamic = [
    "version",
]
authors = [
    { name = "Arnaud Duvermy", email = "arnaud.duvermy@inria.fr" },
    { name = "Thomas Guyet", email = "thomas.guyet@inria.fr" },
]

[project.optional-dependencies]
sql = [
    "connectorx",
]
test = [
    "pytest",
    "syrupy",
    "pytest-cov",
]

[project.license]
file = "LICENSE.txt"

[project.urls]
Homepage = "https://github.com/TanaT-Lab/TanaT"
Source = "https://github.com/TanaT-Lab/TanaT"
Documentation = "https://tanat-lab.github.io/TanaT"
Issues = "https://github.com/TanaT-Lab/TanaT/issues"

[tool.hatch.version]
source = "vcs"

[tool.hatch.version.raw-options]
version_scheme = "no-guess-dev"
local_scheme = "no-local-version"

[tool.hatch.build.hooks.vcs]
version-file = "src/tanat/version.py"
template = """
#!/usr/bin/env python3
'''
Version file automatically generated by setuptools-scm via hatch-vcs.
https://pypi.org/project/setuptools-scm/
https://github.com/ofek/hatch-vcs
Do not track this file and do not edit it manually.
'''
VERSION = {version!r}
VERSION_TUPLE = {version_tuple!r}
"""

[tool.pylint."MESSAGES CONTROL"]
max-line-length = 100
